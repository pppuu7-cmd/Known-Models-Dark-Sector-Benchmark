# M23 native IDM-DR / DAO provider scope audit — 2026-09-11

## Provider
Primary solver: `lesgourg/class_public@e85808324f51fc694d12e3ed7439552a3c3f9540`.

The pinned source contains a source-complete interacting-DM / interacting-dark-radiation interface:
- interacting DM amount via `Omega_idm`, `omega_idm`, or `f_idm`;
- interacting DR amount via exactly one of `N_idr`, `N_dg`, or `xi_idr`;
- DM-DR coupling via exactly one of `a_idm_dr`, `a_dark`, or `Gamma_0_nadm`;
- perturbation/thermodynamic machinery includes the IDM-DR scattering opacity and dark-tight-coupling approximation.

For the NADM parameterization, CLASS documents
`Gamma_DM-DR = -(1+z) Gamma_0_nadm`
and maps it to the internal `a_idm_dr` rate. Passing `Gamma_0_nadm` fixes the interaction temperature index to the NADM convention (`n_index_idm_dr=0`).

## Reference-map audit
A scientifically valid K1 reference for the *interaction mechanism* keeps the dark-sector species content fixed and sends only the interaction rate to zero.

Therefore M23 K1 must NOT compare finite IDM-DR scattering directly with ordinary LambdaCDM while simultaneously deleting DR. The K1 decoupling reference is instead:
- fixed interacting-DM fraction/species assignment;
- fixed nonzero dark-radiation density;
- identical cosmology and radiation nature;
- `Gamma_0_nadm = 0` or coupling omitted.

This reference is an uncoupled DM + DR cosmology, not pure LambdaCDM. A later family-level comparison to LambdaCDM belongs to K6/K7 after the DR-density direction and nuisance/comparator manifold are handled explicitly.

## Source-bound anchor
An independent public FisherLens configuration tied to arXiv:2307.01662 uses a DRDM benchmark with:
- `f_idm = 1`;
- `N_idr = 0.4290`;
- `Gamma_0_nadm = 2.371e-8 Mpc^-1`;
- omega_c h^2 = 0.1197, omega_b h^2 = 0.0222, H0 = 67.5, A_s = 2.196e-9, n_s = 0.9655, tau = 0.06.

This is used only to anchor a representative executable point and parameter scale. It is not treated as an observational best-fit authority for KMDSB.

## Family boundary M23 vs M24
M23 is scoped to the response family generated by simple DM-DR scattering / dark-acoustic-oscillation dynamics at fixed NADM temperature law. M24 (ETHOS-like dark sector) remains a broader family because the general ETHOS interface can vary temperature-law index, mediator-dependent angular coefficients and DR self-interaction structure. A successful M23 test therefore does not represent the full M24 family.

## K2 geometry scope
The physical scattering-rate coordinate is treated as one-sided `Gamma_0_nadm >= 0`. No +/- quotient is inferred from the solver accepting or rejecting numerical values; a negative opacity is not promoted to a physical interaction branch without an independent physical model.

## Scientific boundary
Provider executability or K1 decoupling does not establish K3-K9. In particular, no DAO observational uniqueness, fit quality, model preference, or physical falsification follows from this audit.
