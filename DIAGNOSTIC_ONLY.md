# Diagnostic-only status

The parent preregistration `protocol/W04_M24_IDR_COMPATIBLE_PK_REF_DIAGNOSTIC_v0.1.md` requires `tail_over_top <= 0.25` before a full compatible-pk_ref K1 confirmation may be authorized. The successful ur30 compatibility diagnostic returned `tail_over_top ~= 0.7897317664`, so this branch is NOT authorized for K1 promotion.

Any running or completed full-ladder result from this branch is exploratory/diagnostic evidence only. It must not change the frozen gate, must not promote K1, and must retain `physical_falsification=false`.
