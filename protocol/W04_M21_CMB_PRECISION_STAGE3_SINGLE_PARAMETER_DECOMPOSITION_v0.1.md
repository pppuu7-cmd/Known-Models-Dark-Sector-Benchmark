# W04 M21 CMB precision stage-3 single-parameter decomposition v0.1

Frozen: 2026-09-14 after terminal stage-2 run `34872522336` and before any stage-3 execution.

Provider: `lesgourg/class_public@e85808324f51fc694d12e3ed7439552a3c3f9540`.

Parent authority:
- `waves/wave_04_dark_matter/M21_CMB_PRECISION_STAGE2_SUBGROUP_DECOMPOSITION_TERMINAL.json`
- artifact `10360228613`, digest `sha256:2a6499bcafdd321a5f3832870657aca12210e5f5a64f1b3dc429b56cbbc24e7c`
- parent classification `M21_CMB_PRECISION_STAGE2_COMPLETE`.

## Hypothesis

The numerical CMB excursion localized by stage-2 may be attributable to one or more individual precision assignments inside the two sufficient stage-2 subgroups, G1A and G2B. This is a numerical localization test only. It is not a search for a preferred tuning and cannot promote K1/K3/K4.

## Activation

Stage-3 is authorized only because the terminal parent records exactly:
- G1A: `SUBGROUP_REMOVES_EXCURSION`, Emax `0.03431664516144772`;
- G2B: `SUBGROUP_REDUCES_EXCURSION`, Emax `34.304759775112494`.

No parameter from insufficient G1B/G1C/G2A/G2C is tested here. No parameter is selected after seeing stage-3 values: all 13 assignments below are mandatory and independent.

## Common baseline

Every parameter lane starts from exactly:
1. pinned `class/cl_permille.pre`;
2. `verification/m21/m21_ncdm_tight.pre`;
3. `evolver=0`;
4. exactly one stage-3 parameter assignment below.

The physical cases remain exactly `ref/f2/f3/f4` generated by `verification/m21/mixed_cold_warm_k1_reference.py prepare`. Cosmology, provider pin, gauge, modes, outputs, k-range, l_max and dark-matter fractions are unchanged.

Serialization uses unique-key last-frozen-assignment semantics. Duplicate-key ambiguity is forbidden. The exact conflict set is frozen prospectively: all lanes have no conflict except `G2B__hyper_flat_approximation_nu`, which must replace the inherited `hyper_flat_approximation_nu=7000.` with `1.e6` and record that exact single conflict.

## Mandatory G1A parameter lanes

- `G1A__recfast_Nz0`: `recfast_Nz0=100000`
- `G1A__tol_thermo_integration`: `tol_thermo_integration=1.e-5`
- `G1A__recfast_x_He0_trigger_delta`: `recfast_x_He0_trigger_delta=0.01`
- `G1A__recfast_x_H0_trigger_delta`: `recfast_x_H0_trigger_delta=0.01`

## Mandatory G2B parameter lanes

- `G2B__l_logstep`: `l_logstep=1.026`
- `G2B__l_linstep`: `l_linstep=25`
- `G2B__hyper_sampling_flat`: `hyper_sampling_flat=12.`
- `G2B__hyper_sampling_curved_low_nu`: `hyper_sampling_curved_low_nu=10.`
- `G2B__hyper_sampling_curved_high_nu`: `hyper_sampling_curved_high_nu=10.`
- `G2B__hyper_nu_sampling_step`: `hyper_nu_sampling_step=10.`
- `G2B__hyper_phi_min_abs`: `hyper_phi_min_abs=1.e-10`
- `G2B__hyper_x_tol`: `hyper_x_tol=1.e-4`
- `G2B__hyper_flat_approximation_nu`: `hyper_flat_approximation_nu=1.e6`

## Parallel execution

Run 13 independent parameter jobs in parallel. Each parameter job executes the immutable `ref/f2/f3/f4` cases sequentially using the same built exact-pin binary. Each CLASS execution has a hard 2700-second timeout and the job has a finite timeout. A timed-out/nonzero/missing/nonfinite case makes only that parameter lane `PARAMETER_BLOCKED`; it is not physical model failure.

No parameter job may inspect another parameter's values before execution. The aggregate waits for all required artifacts.

## Frozen metric

Use unchanged `integrator_branch_diagnostic.response_profile` metrics. The frozen parent numerical scale is:

`Emax_parent_RK = 534.8355868817356`.

For each parameter define `Emax_parameter=max(E_TT,E_EE,E_TE)`.

Parameter classifications:
- `PARAMETER_REMOVES_EXCURSION` iff TT, EE and TE excursion factors are all <= 3;
- `PARAMETER_REDUCES_EXCURSION` iff removal fails but `Emax_parameter <= Emax_parent_RK/3`;
- `PARAMETER_INSUFFICIENT` otherwise;
- invalid provenance, provider nonzero return, missing/nonfinite products or inconsistent serialization -> `PARAMETER_BLOCKED`.

Within each parent subgroup, record all sufficient parameters (`REMOVES` or `REDUCES`) without preference.

Subgroup stage-3 classification:
- if any mandatory lane is blocked: `<SUBGROUP>_STAGE3_BLOCKED`;
- if one or more parameters are sufficient: `<SUBGROUP>_STAGE3_SINGLE_PARAMETER_SUFFICIENCY_IDENTIFIED`;
- if no parameter is sufficient while the stage-2 subgroup was sufficient: `<SUBGROUP>_STAGE3_WITHIN_SUBGROUP_INTERACTION_REQUIRED`.

Global classification:
- `M21_CMB_PRECISION_STAGE3_COMPLETE` iff both G1A and G2B are terminal and unblocked;
- otherwise `M21_CMB_PRECISION_STAGE3_BLOCKED`.

## Positive controls

- exact provider commit must equal the frozen pin in every lane;
- all four physical INI hashes for a given case must be identical across parameter lanes;
- each serialized precision profile must contain exactly one stage-3 assignment beyond the common baseline and match the frozen key/value;
- all output products must be finite and uniquely discoverable.

## Negative controls

- no G1B/G1C/G2A/G2C parameter may appear;
- no stage-3 profile may contain two stage-3 assignments;
- no threshold, dark-matter fraction, cosmological parameter, integrator, gauge, output, k range or provider source may change;
- no result-dependent parameter selection is allowed.

## Interpretation ceiling

A stage-3 PASS only identifies individual numerical precision assignments that are sufficient under this frozen benchmark. It does not prove a CLASS bug, does not identify a unique cause if multiple parameters are sufficient, does not justify production tuning, does not establish physical convergence globally, does not promote M21 K1/K3/K4, and is not a physical falsification or validation of mixed cold+warm dark matter. If no single parameter is sufficient, the correct result is within-subgroup interaction required.
