# W04 M19 axionCAMB2 exact-zero bypass + neutrino-off isolation preregistration v0.1

Date: 2026-09-10
Provider: `dgrin1/axionCAMB@891e779cc0bd422e49f97533e6c2fc761149737d`
Status: FROZEN_BEFORE_EXECUTION
Scientific promotion: NONE

## Question

The already frozen exact-zero bypass recovery removes the scalar shooting/log-zero path and guards the unphysical exact-zero axion recombination spline derivative, but its exact-zero cases still terminate in legacy `MassiveNu::Nu_rho`. The independently executed neutrino-off control on the unpatched provider still terminates before useful outputs because the scalar exact-zero singularity remains. This gate tests the prospectively defined conjunction of those two already independently motivated infrastructure controls; it is not a new physical model and cannot establish K1.

## Frozen provider and physics scope

- exact provider SHA: `891e779cc0bd422e49f97533e6c2fc761149737d`;
- use the previously audited minimal zero-bypass patch from `verification/m19/axioncamb2_zero_bypass_recovery.py` unchanged;
- exact-zero fraction case: `use_axfrac=T`, `axfrac=0`;
- exact-zero density case: `use_axfrac=F`, `omaxh2=0` as generated by the existing zero-probe harness;
- for the two exact-zero isolation cases only, set `omnuh2=0`, `massive_neutrinos=0`, `nu_mass_eigenstates=0`;
- no epsilon axion density, no IC tuning, no altered axion mass, no threshold changes after output inspection;
- finite-axion non-interference control remains `axfrac=0.10` with the provider's original neutrino settings and must agree between patched and untouched providers.

The neutrino-off exact-zero workload is intentionally a changed comparator cosmology. Therefore even a perfect execution/output match between the two zero parameterizations is infrastructure localization only, not a LambdaCDM/CDM reference proof.

## Frozen gates

A scoped infrastructure PASS requires all of:

1. untouched and patched provider builds exit 0;
2. patched exact-zero fraction + neutrino-off exits 0 and emits finite nonempty `scalCls.dat`, `matterpower.dat`, and `transfer_out.dat`;
3. patched exact-zero density + neutrino-off exits 0 and emits the same products;
4. the two exact-zero parameterizations agree at symmetric relative tolerance <= `1e-10` on matched numeric products;
5. patched finite `axfrac=0.10` and untouched finite `axfrac=0.10` both exit 0 and agree at symmetric relative tolerance <= `1e-10` on matched numeric products;
6. runtime-check build of patched exact-zero fraction + neutrino-off exits 0.

If any exact-zero isolation case fails, classification is `M19_ZERO_BYPASS_NU0_ISOLATION_BLOCKED`; this remains a provider/infrastructure result, not a physical failure.

If all pass, classification is `M19_ZERO_BYPASS_NU0_ISOLATION_PASS_WITH_SCOPE`. This only localizes the remaining exact-zero crash to the legacy massive-neutrino interaction/path under this provider. It does **not** promote K1-K9.

## Next gate if PASS

Preregister a reference comparison against an independently pinned pure-CAMB/CDM implementation using an explicitly matched no-massive-neutrino cosmology first. Only after that matched comparator is source-bound and prospectively frozen may an M19 K1 reference claim be attempted.

## Next gate if BLOCKED

Consume runtime-check traceback and audit the next first failing consumer. Do not patch additional physics or insert epsilon axion density post hoc. If no provider-supported exact-zero route exists after bounded diagnostics, classify this provider's exact-zero K1 path as `BLOCKED_IMPLEMENTATION_REFERENCE_PROVIDER` and move to another independent FDM/ULA provider or a published-equation verification implementation.
