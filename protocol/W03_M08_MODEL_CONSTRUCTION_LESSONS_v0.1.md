# Wave 03 M08 model-construction lessons v0.1

Updated: 2026-09-09  
Status: LIVING / evidence-fed from M08 CPL adversarial comparison

This supplement records methodology changes generated by the M08 CPL benchmark and the DSIR authority delta AD-002. It supplements `FUTURE_MODEL_CONSTRUCTION_METHODOLOGY_v0.1.md` and `W03_MODEL_CONSTRUCTION_LESSONS_v0.1.md`.

## M08-L1 — attack the nearest family manifold, not one representative ray

M07 canonical quintessence was clearly non-collinear with constant-w C1 and showed a cross-channel P/H residual when one C1 amplitude was shared across both blocks.

That was not sufficient.

M08 introduced the local two-parameter CPL family

`w(a)=w0+wa(1-a)`

and fitted one shared parameter vector across the same 35-node low-k P block plus 7-node H block.

Same-solver constant-w-like residual fractions for M07 `dr/dq`:
- combined P+H: `15.82%`;
- P: `12.06%`;
- H: `29.15%`.

Two-dimensional CPL residual fractions:
- combined P+H: `1.107%`;
- P: `0.864%`;
- H: `1.993%`.

Thus a separator that survives one representative comparator ray can disappear almost completely after profiling the local span of the same broader phenomenological family.

**Construction rule:** before calling a future model's signature novel, profile the strongest implemented local comparator manifold, not merely the nearest hand-picked benchmark direction.

This is DP-0815.

## M08-L2 — parameter dimension is not response dimension

The M08 CPL parameters `epsilon0=1+w0` and `wa` are formally two independent model coordinates, but their combined P/H response directions have

- angle `9.179 deg`;
- singular values `{1.88297,0.09453}`;
- `sigma2/sigma1 = 0.05020`.

Therefore the local response is strongly anisotropic and nearly one-dimensional even though the parameterization is two-dimensional.

The weak second direction nevertheless matters scientifically because it removes most of the M07 cross-channel residual.

**Construction rule:** rank/geometry is measured from the response operator; it is never inferred from parameter count. Weak singular directions must not be discarded merely because they carry less norm if they are the directions that break or create a comparator degeneracy.

This independently reinforces DP-0303.

## M08-L3 — observational comparison needs one exact common operator, not just one covariance

DSIR authority delta AD-002 introduces the frozen Article-2 G5 data-whitened cross-family contract at

`Dark-Sector-Influence-Reconstruction@864952e1520d82473a9e976edfeb69f9899d174d`.

The contract makes the following distinction mandatory:

- having a real covariance is not enough;
- every compared family/model row must be propagated through one prospectively frozen observation/operator bridge into the exact same coordinate vector on which that covariance acts;
- coordinate order, units, masks and immutable provenance are part of the test;
- no zero imputation, output-dependent regularization or post-hoc rank cutoff is allowed.

The existing ACT x unWISE 26-coordinate covariance/operator chain is a promising real bridge, but DSIR explicitly records that the multi-family provider-to-exact-26-coordinate matrix is not yet bound. Therefore it cannot yet be used as a classifying cross-family G5 execution.

**Construction rule:** for a future original model, observation-space novelty must be measured only after both the candidate and the comparator families pass through the identical instrument/survey operator and covariance coordinates.

This is DP-0816.

## M08-L4 — closure conventions are scientific provenance

The first M08 Actions run failed before cosmological evolution because both `Omega_Lambda=0` and `Omega_fld` were explicitly specified, while pinned CLASS requires one closure component to remain inferred.

The corrected branch fixes

`Omega_Lambda=0`

and lets `Omega_fld` be inferred from closure.

The corrected run passes; the original failure remains preserved as an implementation/configuration failure.

**Construction rule:** cosmological budget closure, normalization and which component is solver-inferred are explicit inputs to the benchmark. An inconsistent closure assignment is not a physical-domain failure of the model.

This is DP-0817.

## M08-L5 — microphysical elegance is especially vulnerable to flexible phenomenological absorption

M07 has real mathematical structure that constant-w does not:
- canonical field dynamics;
- exact field-reflection quotient;
- natural local coordinate `q=lambda^2`;
- prospective within-family holdout support.

Yet its tested P/H response is reproduced to about one percent by a mild local CPL mapping.

For M07 `lambda=0.30`, the linear mapped CPL point is approximately

`w0=-0.98734`, `wa=-0.01806`.

This is not an extreme phenomenological deformation.

**Construction implication:** a future original dark-sector model should be designed to produce at least one controlled signature outside the flexible smooth-DE response span. A new action, field or interpretation is not enough if survey observables can be reabsorbed by a modest `w(a)` history.

Promising escape directions remain:
- robust metric/slip observables after gauge/representation audit;
- characteristic scale dependence not representable by smooth DE;
- temporal/scale cross-relations that use one parameter mapping across all blocks;
- nonlinear or high-k structure when physically controlled;
- tensor/GW or coupling channels when defined;
- prospective relations that flexible comparator families fail on a withheld regime.

## M08-L6 — negative comparator results are constructive evidence

The M08 result weakens a candidate novelty claim but strengthens the model-construction methodology.

Do not delete or minimize such results. They specify where a future model **must not** place its only signature.

M07 remains useful as a predictive benchmark family; what is lost is the claim that its constant-w P/H separator is mechanism-specific.

## Immediate methodology frontier

The next useful question is no longer whether CPL can fit M07 P/H locally; it can.

The next high-value tests are:

1. propagate M07 and M08 through one exact common observational bridge under AD-002;
2. search for a gauge-robust metric/scale/time response not contained in the CPL local span;
3. test whether M08 itself is observationally rank-two after whitening rather than merely theory-rank-two;
4. use subsequent W03 mechanisms to decide which current design priors become `REINFORCED` or `CORE` rather than continually adding untested requirements.
