# W03 M13b K3D2-B conditional restart + floating-boundary recovery v0.1

Frozen: 2026-09-14 after run 34787545806 and before executing this recovery.

Provider: `lesgourg/class_public@64bbab707faf4de4779a9e04edd180fef18d98fa`.
Parent: `protocol/W03_M13B_K3D2B_EXACT_TWO_SEGMENT_BACKGROUND_RESTART_PROBE_v0.1.md`.

## Observations fixed by this recovery

Run 34787545806 established two implementation facts without evaluating B1/B2/B3:

1. unconditional two-segment NDF15 is numerically distinguishable from untouched upstream in the null LambdaCDM control: normalized P(k) L2 = `3.763048791515821e-08`, above the prospectively frozen `1e-8` gate, while both provider return codes are zero and H0 agrees exactly;
2. enabled qcf+qpf still fails in the first segment at the handoff endpoint. CLASS reports through stdout an NDF15 minimum-step failure at `x=-1.791759e+00`, the frozen `log(1/6)` boundary. The previous underflow guard inspected stderr only and is therefore a harness bug.

Neither observation is physical falsification.

## Recovery A: conditional restart

The untouched single-call upstream path MUST be retained whenever both `pba->has_qcf` and `pba->has_qpf` are false.

Only when `(pba->has_qcf == _TRUE_) || (pba->has_qpf == _TRUE_)` may the background solve use two NDF15 calls split at the frozen handoff.

Thus the disabled-adapter/null path is byte-semantically the original CLASS integration path. No null-control threshold is changed.

## Recovery B: floating-point ownership of the single handoff point

The segment endpoint remains exactly

`loga_handoff = log(1./6.)`.

CLASS reconstructs the scale factor as `a=exp(loga)`. Therefore the pre-handoff ownership predicate at the isolated endpoint is changed from comparison with an independently rounded literal `1./6.` to comparison with the scale factor generated by the same map:

`a <= exp(log(1./6.))`.

Only the four background handoff guards are changed. For every open interval `a < a_handoff` and `a > a_handoff`, the equations are identical to the previously frozen K3D2-B realization. No smoothing interval, epsilon band, fitted shift, retuning or threshold relaxation is allowed.

Perturbation guards are unchanged in this background-only recovery.

## Recovery C: diagnostic channel

Minimum-step/underflow diagnostics are searched in the concatenation of stdout and stderr, case-insensitively. This changes no numerical behavior.

## Frozen gates

### N: conditional null control

Exact LambdaCDM with qcf=qpf absent must traverse the untouched one-call branch. Require:

- provider rc=0 for upstream and recovered builds;
- finite positive P(k);
- normalized P(k) L2 <= `1e-10` (the existing K3D2-A exact-null standard, stricter than the failed restart-probe threshold);
- relative H0 difference <= `1e-10`;
- source audit confirms one upstream call remains inside the disabled branch and two calls exist only inside the qcf/qpf-enabled branch.

### E: enabled background

Use unchanged frozen K3C1-normalized qcf+qpf input and default NDF15. Require:

- build success and provider rc=0;
- no minimum-step/underflow diagnostic in stdout+stderr;
- finite positive-H background with samples below and above the handoff;
- z=5 interpolation: `|phi-0.92|<=2e-4`, `|psi-1.02|<=2e-4`, `|dphi/dN|<=2e-3`, `|dpsi/dN|<=2e-3`.

B1 crossing/today gates, B2 spectra and B3 perturbation comparison remain unevaluated.

## Classification

Both N and E pass: `M13B_K3D2B_CONDITIONAL_RESTART_BACKGROUND_IMPLEMENTATION_PASS_WITH_SCOPE`, authorizing a separately frozen full regression.

Otherwise: `M13B_K3D2B_CONDITIONAL_RESTART_BACKGROUND_BLOCKED`.

K3 remains PARTIAL. K4/K5 remain closed. No author-code reproduction or physical falsification is claimed.
