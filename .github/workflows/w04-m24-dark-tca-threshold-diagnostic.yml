name: W04 M24 dark-TCA threshold diagnostic

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - '.github/workflows/w04-m24-dark-tca-threshold-diagnostic.yml'

permissions:
  contents: write

jobs:
  diagnose:
    runs-on: ubuntu-latest
    timeout-minutes: 50
    env:
      CLASS_PIN: e85808324f51fc694d12e3ed7439552a3c3f9540
      OMP_NUM_THREADS: '1'
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Install analysis dependency and pin provider
        shell: bash
        run: |
          set -euo pipefail
          python3 -m pip install --disable-pip-version-check --no-input numpy==2.3.3
          git clone -q https://github.com/lesgourg/class_public.git class
          git -C class checkout -q --detach "$CLASS_PIN"
          test "$(git -C class rev-parse HEAD)" = "$CLASS_PIN"
          make -C class -j2 > m24_tca_build.log 2>&1
      - name: Prepare frozen physical cases and numerical profiles
        run: python3 verification/m24/dark_tca_threshold_diagnostic.py prepare m24_tca_cases m24_tca_profiles
      - name: Execute all frozen profiles
        shell: bash
        run: |
          set +e
          : > m24_tca_status.tsv
          mkdir -p m24_tca_results
          for profile in D_DEFAULT E_EARLY_OFF O_TCA_OFF; do
            rm -rf class/output; mkdir -p class/output
            for case in ref zero a18k a6k a1p8k; do
              if [ "$profile" = D_DEFAULT ]; then
                (cd class && ./class ../m24_tca_cases/${case}.ini > ../m24_tca_${profile}_${case}.log 2>&1)
              else
                (cd class && ./class ../m24_tca_cases/${case}.ini ../m24_tca_profiles/${profile}.pre > ../m24_tca_${profile}_${case}.log 2>&1)
              fi
              rc=$?
              printf '%s__%s\t%d\n' "$profile" "$case" "$rc" >> m24_tca_status.tsv
            done
            rm -rf m24_tca_results/$profile
            cp -a class/output m24_tca_results/$profile
          done
          python3 - <<'PY'
          import json
          d={}
          for line in open('m24_tca_status.tsv'):
              k,v=line.rstrip().split('\t'); d[k]=int(v)
          open('m24_tca_status.json','w').write(json.dumps(d,indent=2,sort_keys=True)+'\n')
          PY
          cat m24_tca_status.json
          exit 0
      - name: Analyze frozen diagnostic
        run: |
          python3 verification/m24/dark_tca_threshold_diagnostic.py analyze m24_tca_results m24_tca_status.json m24_tca_result.json
          cat m24_tca_result.json
      - name: Persist canonical diagnostic
        shell: bash
        run: |
          set -euo pipefail
          cp m24_tca_result.json /tmp/result.json
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git fetch origin main
          git reset --hard origin/main
          mkdir -p models/ethos_like_dark_sector waves/wave_04_dark_matter
          cp /tmp/result.json models/ethos_like_dark_sector/M24_DARK_TCA_THRESHOLD_DIAGNOSTIC_RESULT.json
          cp /tmp/result.json waves/wave_04_dark_matter/M24_DARK_TCA_THRESHOLD_DIAGNOSTIC_RESULT.json
          git add models/ethos_like_dark_sector/M24_DARK_TCA_THRESHOLD_DIAGNOSTIC_RESULT.json waves/wave_04_dark_matter/M24_DARK_TCA_THRESHOLD_DIAGNOSTIC_RESULT.json
          git diff --cached --quiet || { git commit -m 'Record M24 dark-TCA threshold diagnostic'; git pull --rebase origin main; git push origin HEAD:main; }
      - name: Enforce diagnostic-only semantics
        run: |
          python3 - <<'PY'
          import json
          r=json.load(open('/tmp/result.json'))
          allowed={'M24_DARK_TCA_DIAGNOSTIC_PROVIDER_BLOCKED','M24_DARK_TCA_EXCURSION_LOCALIZED','M24_DARK_TCA_EXCURSION_STRONGLY_SENSITIVE','M24_DARK_TCA_EXCURSION_INSENSITIVE'}
          assert r['classification'] in allowed
          assert r['K1_promoted'] is False
          assert r['physical_falsification'] is False
          PY
      - name: Upload evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: w04-m24-dark-tca-threshold-diagnostic
          path: |
            m24_tca_build.log
            m24_tca_cases/
            m24_tca_profiles/
            m24_tca_status.json
            m24_tca_status.tsv
            m24_tca_result.json
            m24_tca_*.log
            m24_tca_results/
          retention-days: 90
